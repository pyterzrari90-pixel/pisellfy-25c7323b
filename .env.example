# Pi Network environment — Mainnet only (real payments)

# oAuth Client ID (public value)
VITE_PI_CLIENT_ID_MAINNET=ScGmmvjKDYywIvAtSqX-XJcGskGotJm2tt22HP_S8Ss

# Server-side Pi API key is stored as a backend secret (PI_NETWORK_API_KEY),
# never here and never with a VITE_ prefix.
